version: "2"

linters:
  default: standard
  disable:
    - errcheck
  settings:
    govet:
      enable-all: true
      disable:
        - fieldalignment
        - shadow

run:
  timeout: 5m

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
